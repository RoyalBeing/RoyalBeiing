<section class="section newsletter" {{ section.shopify_attributes }}>
  <div class="wrap center">
    <p class="eyebrow" style="color:var(--rb-gold-500)">{{ section.settings.eyebrow }}</p>
    <h2>{{ section.settings.heading }}</h2>
    <p>{{ section.settings.text }}</p>
    {% form 'customer', class: 'news-form' %}
      <input type="hidden" name="contact[tags]" value="newsletter">
      <input type="email" name="contact[email]" required placeholder="{{ section.settings.placeholder }}" aria-label="Email address">
      <button class="btn btn-gold" type="submit">{{ section.settings.button_label }}</button>
      {%- if form.posted_successfully? -%}
        <p class="form-status form-status--success" style="width:100%">{{ section.settings.success }}</p>
      {%- endif -%}
      {%- if form.errors -%}
        <p class="form-status form-status--error" style="width:100%">{{ form.errors | default_errors }}</p>
      {%- endif -%}
    {% endform %}
  </div>
</section>

{% schema %}
{
  "name": "Newsletter",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "Join the Ritual" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "Become a Royal Being" },
    { "type": "textarea", "id": "text", "label": "Text", "default": "Early access to new bars, small-batch restocks, and the occasional ritual note." },
    { "type": "text", "id": "placeholder", "label": "Input placeholder", "default": "Your email address" },
    { "type": "text", "id": "button_label", "label": "Button", "default": "Subscribe" },
    { "type": "text", "id": "success", "label": "Success message", "default": "Welcome to the ritual — please check your inbox." }
  ],
  "presets": [{ "name": "Newsletter" }]
}
{% endschema %}

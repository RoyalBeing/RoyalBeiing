<section class="section" style="background:var(--rb-mist)" {{ section.shopify_attributes }}>
  <div class="wrap">
    <div class="sec-head center reveal">
      {%- if section.settings.eyebrow != blank -%}<p class="eyebrow">{{ section.settings.eyebrow }}</p>{%- endif -%}
      <h2>{{ section.settings.heading }}</h2>
      {%- if section.settings.text != blank -%}<p class="lead">{{ section.settings.text }}</p>{%- endif -%}
    </div>
    <div class="product-grid reveal">
      {%- liquid
        assign col = collections[section.settings.collection]
        assign limit = section.settings.products_to_show
      -%}
      {%- if col != blank -%}
        {%- for product in col.products limit: limit -%}
          {% render 'product-card', product: product %}
        {%- endfor -%}
      {%- else -%}
        {%- for product in collections.all.products limit: limit -%}
          {% render 'product-card', product: product %}
        {%- endfor -%}
      {%- endif -%}
    </div>
    {%- if section.settings.button_label != blank -%}
      <div class="center" style="margin-top:40px">
        <a class="btn btn-outline" href="{{ section.settings.button_link | default: routes.all_products_collection_url }}">{{ section.settings.button_label }}</a>
      </div>
    {%- endif -%}
  </div>
</section>

{% schema %}
{
  "name": "Featured products",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "FEATURED RITUALS" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "Find Your New Favourite Bar" },
    { "type": "textarea", "id": "text", "label": "Text", "default": "Explore a curated selection of Royal Being herbal soaps, chosen from across our collections. Each bar offers its own combination of aroma, ingredients, character, and cleansing experience." },
    { "type": "collection", "id": "collection", "label": "Collection (optional — leave blank for all)" },
    { "type": "range", "id": "products_to_show", "min": 4, "max": 16, "step": 1, "label": "Products to show", "default": 8 },
    { "type": "text", "id": "button_label", "label": "Button label", "default": "SHOP ALL SOAPS" },
    { "type": "url", "id": "button_link", "label": "Button link" }
  ],
  "presets": [{ "name": "Featured products" }]
}
{% endschema %}
